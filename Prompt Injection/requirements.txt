torch==2.2.0  # Ensure compatibility with your GPU (use +cu118 for CUDA 11.8)
transformers==4.38.2
datasets==2.18.0
scikit-learn==1.3.0
numpy==1.26.2
scipy==1.11.3
tuned-lens==1.0.0  # If this is an internal library, you may need to install it differently
